Sprint 2

- try/except deve envolver apenas o código que pode falhar.
- Serviços são responsáveis por tratar seus próprios erros.
- Asking the AI to be more actionable can cause false precision when reliable context is missing.

Specific-sounding advice is not always reliable advice.

The AI successfully used trusted venue context from local JSON data.

However, grounding one venue fact does not prevent the model from generating unsupported details in other sections.

Reliable context must be expanded and the model must clearly distinguish trusted venue facts from general guidance.